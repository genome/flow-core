#!/usr/bin/env python

import logging
import sys
import flow.commands.base

LOG = logging.getLogger()

if __name__ == '__main__':
    argv = sys.argv
    argv.pop(0)   # remove the command name
    rv = flow.commands.base.run(argv, LOG)
    sys.exit(rv)
